# pamuk

## Dependencies

This project depends on the following ROS packages:

- gazebo_ros
- xacro
- turtlebot3_description
- turtlebot3_bringup
- map_server
- tf
- rviz
- move_base

Make sure to have these packages installed before trying to launch this project.

## Installation

To install the dependencies, run the following command:

sudo apt-get install ros-noetic-gazebo-ros 
sudo apt-get install ros-noetic-xacro 
sudo apt-get install ros-noetic-turtlebot3-description 
sudo apt-get install ros-noetic-turtlebot3-bringup 
sudo apt-get install ros-noetic-map-server 
sudo apt-get install ros-noetic-tf 
sudo apt-get install ros-noetic-rviz 
sudo apt-get install ros-noetic-move-base

sudo nano ~/.bashrc
export TURTLEBOT3_MODEL=waffle
export GAZEBO_MODEL_PATH=/home/halilpamuk24/catkin_ws/src/pamuk
echo $GAZEBO_MODEL_PATH

build it using catkin_make or catkin build.

Usage
To launch this project, run the following command:

roslaunch pamuk <launch_file>
